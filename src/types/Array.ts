import {ElementStates} from "./element-states";

export type TArray = {
    state: ElementStates;
    item?: string | number;
}

export type TSorrtArray = {
    state: ElementStates,
    item: number
}